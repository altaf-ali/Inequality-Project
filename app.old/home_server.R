library(countrycode)

home_server1 <- function(input, output, session) {
  
  # message("GEKKI")
  # 
  # updateSelectInput(session,
  #                   "country", 
  #                   choices = setNames(as.character(countrycode_data$iso3c), countrycode_data$country.name))
}